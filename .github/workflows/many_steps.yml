name: testing workflow
on: 
  workflow_dispatch:

jobs:
   job_A:
    runs-on: ubuntu-latest
    steps:
      - run : echo "first step"
   job_B:
     runs-on: ubuntu-latest
     needs: job_A
     steps:
       - run: echo "seconde step"
   job_C:
     runs-on: ubuntu-latest
     needs: job_A
     steps:
       - run: echo "Third step"
   job_D:
    runs-on: ubuntu-latest
    needs: [job_B,job_C]
    steps:
      - run: echo "last step"
